// MEMORY.md Section: Function Parameters with 'as val' (copy on call)
// Use 'as val' to receive a copy - changes don't affect the caller's original
// NOTE: This demonstrates the as val syntax - copy semantics

fn show_copy(arr: int[] as val): void =>
    // arr is meant to be a COPY of the caller's array
    print("Inside function with as val, arr = ")
    print(arr)
    print("\n")
    // The 'as val' modifier indicates copy semantics
    // Caller's array should remain unchanged

fn main(): void =>
    var nums: int[] = {1, 2, 3}
    print("Before function call: ")
    print(nums)
    print("\n")

    show_copy(nums)

    print("After function call: ")
    print(nums)
    print("\n")
